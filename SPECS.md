# faas-template-r-function

Pass in a number and this function will add 1 and return.

## Parameters

A JSON object containing:
- `number` - {Integer} Required. Number to add 1 to
 

## Constraints

- Only accepts a single number

## Response

`number` plus 1.